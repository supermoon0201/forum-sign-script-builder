interface:
  display_name: "Forum Sign Builder"
  short_description: "Build reliable forum sign scripts"
  default_prompt: "Use -sign-script-builder to add a new site sign-in script in the current repository."
